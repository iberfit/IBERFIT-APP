/* IBERFIT V8.4 · Coach OS final: seguridad reforzada, sesiones revocables, editor de bloques y QA de despliegue */
import { IBERFIT_CONFIG, resolveApiMode } from "./config.js";

const $ = (q, root = document) => root.querySelector(q);
const $$ = (q, root = document) => [...root.querySelectorAll(q)];
const root = $("#coachRoot");
const COACH_KEY = "IBERFIT_COACH_SESSION_V8_4";
const estado = { token:"", vence:"", coach:{}, panel:null, espacio:null, clienteId:"", aviso:"", avisoTipo:"info", enlace:"", pestaña:"operacion", filtroEjercicio:"", filtroPatron:"", filtroZona:"", filtroNivel:"" };

function html(v="") { return String(v ?? "").replace(/[&<>'"]/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c])); }
function fecha(v) { if (!v) return "—"; const d = new Date(v); return Number.isNaN(d.getTime()) ? html(v) : d.toLocaleString("es-CL", { day:"2-digit", month:"2-digit", year:"numeric", hour:"2-digit", minute:"2-digit" }); }
function leerSesion() { try { const raw=sessionStorage.getItem(COACH_KEY); if (!raw) return null; const s=JSON.parse(raw); if (!s.coachToken) return null; if (s.expiresAt && Date.now() > new Date(s.expiresAt).getTime()) { sessionStorage.removeItem(COACH_KEY); return null; } return s; } catch { return null; } }
function guardarSesion(data) { const s={ coachToken:data.coachToken || "", expiresAt:data.expiresAt || "", coach:data.coach || {} }; sessionStorage.setItem(COACH_KEY, JSON.stringify(s)); estado.token=s.coachToken; estado.vence=s.expiresAt; estado.coach=s.coach; }
async function api(action, payload={}) { if (resolveApiMode() === "mock") return mock(action, payload); try { const res=await fetch(IBERFIT_CONFIG.apiPath, { method:"POST", headers:{"Content-Type":"application/json"}, credentials:"same-origin", body:JSON.stringify({ action, coachToken:estado.token, baseUrl:window.location.origin, ...payload }) }); const text=await res.text(); try { return JSON.parse(text); } catch { return { ok:false, status:"ERROR_LECTURA", message:text.slice(0,300) }; } } catch(e) { return { ok:false, status:"ERROR_CONEXION", message:"No pudimos conectar con IBERFIT." }; } }
function mock(action, payload) { const panel={ metrics:{ clientes:1, accesosPendientes:1, semanasPublicadas:1, sesionesPublicadas:3, feedbackPendiente:1, checkinPendiente:1, alertas:1, sesionesActivas:1 }, clients:[{id:"APPCLI-0001",name:"Alejandro",email:"alejandro.demo@email.com",modality:"HIBRIDO",objective:"Fuerza y composición corporal",status:"ACTIVO",accessStatus:"ACTIVO",activeWeekTitle:"Semana 1 · Base técnica",iri:"62",iriClass:"PERFORMANCE",pendingFeedback:1,pendingCheckin:1}], alerts:[{id:"ALT-0001",clientId:"APPCLI-0001",priority:"MEDIA",message:"Check-in pendiente de revisar"}], exerciseLibrary:[{id:"EJ-0001",name:"Sentadilla goblet",pattern:"Sentadilla",zone:"Tren inferior",material:"Kettlebell",level:"Base",objective:"Fuerza técnica"},{id:"EJ-0002",name:"Peso muerto rumano",pattern:"Bisagra",zone:"Tren inferior",material:"Mancuernas",level:"Base",objective:"Fuerza posterior"}], sessionTemplates:[{id:"PLSES-0001",name:"Fuerza base tren inferior",type:"Fuerza",deliveryMode:"AUTONOMA",duration:"45",objective:"Construir base técnica",criterion:"Progresar solo si RPE y recuperación acompañan.",preparation:"Movilidad + activación 8 min",mainBlock:"Sentadilla goblet 3x10\nPeso muerto rumano 3x8",coolDown:"Respiración 5 min",indications:"Mantener técnica y registrar RPE",material:"Mancuernas o kettlebell"}] }; if (action==="coachLogin") return {ok:true,data:{coachToken:"MOCK",expiresAt:new Date(Date.now()+3600000).toISOString(),coach:{name:"Carlos",login:"carlos"},dashboard:panel}}; if (action==="coachGetDashboard") return {ok:true,data:panel}; if (action==="coachGetClientWorkspace") return {ok:true,data:mockEspacio(payload.clientId || "APPCLI-0001")}; if (action==="coachCrearCliente") return {ok:true,data:{clientId:"APPCLI-0002",activationLink:window.location.origin+"/activar.html?token=mock",expiresAt:new Date(Date.now()+86400000).toISOString()}}; if (action==="coachGenerarEnlaceActivacion") return {ok:true,data:{activationLink:window.location.origin+"/activar.html?token=mock-activacion"}}; if (action==="coachGenerarEnlaceRecuperacion") return {ok:true,data:{resetLink:window.location.origin+"/activar.html?token=mock-reset"}}; return {ok:true,data:{saved:true,weekId:"SEM-0001",sessionId:"SES-0009",revoked:true}}; }
function mockEspacio(id) { const blocks=[{title:"Preparación",focus:"Activación general",exercises:[{name:"Movilidad cadera",time:"6 min",note:"Rango cómodo"}]},{title:"Fuerza principal",focus:"Calidad técnica",exercises:[{name:"Sentadilla goblet",sets:"3",reps:"10",rest:"90 s",note:"RPE 7"},{name:"Peso muerto rumano",sets:"3",reps:"8",rest:"90 s",note:"Control lumbar"}]}]; return { client:{id,name:"Alejandro",email:"alejandro.demo@email.com",phone:"+56900000000",modality:"HIBRIDO",objective:"Fuerza, adherencia y composición corporal",status:"ACTIVO",accessStatus:"ACTIVO",notes:"Demo"}, plan:{nextAction:"Mantener foco técnico",objective:"Base medible"}, week:{id:"SEM-0001",number:"1",title:"Semana 1 · Base técnica",focus:"Calidad técnica",message:"Semana de construcción",state:"PUBLICADA"}, sessions:[{id:"SES-0001",weekId:"SEM-0001",deliveryMode:"PRESENCIAL",title:"Fuerza base tren inferior",type:"Fuerza",duration:"55",state:"PUBLICADA",objective:"Técnica y tolerancia",criterion:"Progresar si la recuperación acompaña.",preparation:"Activación general 8 min",mainBlock:"Sentadilla goblet 3x10\nBisagra 3x8",coolDown:"Movilidad suave 5 min",indications:"Registrar RPE y molestia",material:"Mancuernas",structure:"",blocksJson:JSON.stringify(blocks),observe:"Técnica y respiración",adjust:"Bajar carga si RPE > 8",report:"RPE, molestia y energía",order:"1"}], iri:{total:"62",classification:"PERFORMANCE",recommendation:"Construir base antes de aumentar carga.",limiters:"Movilidad de cadera y resistencia base",bioimpedance:"Bioimpedancia inicial registrada",status:"PENDIENTE"}, feedback:[{id:"FB-1",sessionId:"SES-0001",realization:"PARCIAL",rpe:"8",fatigue:"7",energy:"5",pain:"2",technique:"6",comment:"Costó la bisagra.",decision:""}], checkin:[{id:"CHK-1",weekId:"SEM-0001",fatigue:"6",energy:"7",pain:"2",comment:"Semana buena.",decision:""}], tokens:[], activeSessions:[{id:"SAC-1",state:"ACTIVA",createdAt:new Date().toISOString(),expiresAt:new Date(Date.now()+3600000).toISOString(),source:"APP_CLIENT"}] }; }
function avisar(msg,tipo="info",enlace="") { estado.aviso=msg||""; estado.avisoTipo=tipo; estado.enlace=enlace||""; render(); }
function avisoHtml() { if (!estado.aviso) return ""; return `<div class="coach-status ${html(estado.avisoTipo)}">${html(estado.aviso)}${estado.enlace ? `<div class="link-box"><input readonly value="${html(estado.enlace)}"><button type="button" class="btn btn-ghost" id="copyLastLink">Copiar</button></div>` : ""}</div>`; }
function loginPantalla() { return `<main class="coach-login-screen"><section class="coach-login-brand"><div class="coach-logo"><img src="/assets/iberfit-isotipo.png" alt="IBERFIT"></div><h1>IBERFIT Coach OS</h1><p>Sistema interno para crear clientes, activar accesos, publicar semanas y revisar la respuesta real.</p></section><form class="coach-login-card" id="coachLoginForm"><h2>Acceso interno</h2><p>Uso exclusivo IBERFIT.</p><label>Usuario<input id="coachLogin" autocomplete="username" placeholder="Usuario"></label><label>Contraseña<input id="coachPassword" type="password" autocomplete="current-password" placeholder="Contraseña"></label><button class="btn btn-primary btn-full" type="submit">Entrar</button>${avisoHtml()}<small>La contraseña no se guarda en texto plano. Acceso protegido por token temporal.</small></form></main>`; }
function shell() { const p=estado.panel || { metrics:{}, clients:[] }; return `<section class="coach-app"><aside class="coach-sidebar"><div class="sidebar-brand coach-side-brand"><div class="sidebar-iso"><img src="/assets/iberfit-isotipo.png" alt="IBERFIT"></div><strong>IBERFIT</strong><span>Coach OS</span></div><nav class="coach-tabs"><button class="coach-tab ${estado.pestaña==="operacion"?"active":""}" data-tab="operacion">Operación</button><button class="coach-tab ${estado.pestaña==="alta"?"active":""}" data-tab="alta">Alta cliente</button><button class="coach-tab ${estado.pestaña==="biblioteca"?"active":""}" data-tab="biblioteca">Biblioteca</button><button class="coach-tab ${estado.pestaña==="iri"?"active":""}" data-tab="iri">IRI</button><button class="coach-tab ${estado.pestaña==="seguridad"?"active":""}" data-tab="seguridad">Seguridad</button></nav><button class="btn btn-ghost btn-full" id="coachRefreshBtn">Actualizar</button><button class="btn btn-link btn-full" id="coachLogoutBtn">Cerrar sesión</button><p class="coach-mini">Versión <strong>V8.4</strong><br>Cliente activo: <strong>${html(estado.espacio?.client?.name || "—")}</strong></p></aside><main class="coach-main"><header class="coach-topbar"><div><span class="section-label compact">Panel interno</span><h1>Ecosistema operativo IBERFIT</h1></div><div class="private-badge">${html(estado.coach?.name || "Coach")}</div></header>${avisoHtml()}${metricas(p.metrics || {})}${estado.pestaña==="alta" ? altaCliente() : estado.pestaña==="biblioteca" ? biblioteca() : estado.pestaña==="iri" ? iriPanel() : estado.pestaña==="seguridad" ? seguridadPanel() : operacion()}</main></section>`; }
function metricas(m) { const items=[["Clientes",m.clientes],["Accesos pendientes",m.accesosPendientes],["Semanas",m.semanasPublicadas],["Sesiones",m.sesionesPublicadas],["Feedback",m.feedbackPendiente],["Check-in",m.checkinPendiente],["Alertas",m.alertas],["Sesiones activas",m.sesionesActivas]]; return `<section class="coach-metrics">${items.map(([l,v]) => `<article class="coach-metric"><strong>${html(v ?? "—")}</strong><span>${html(l)}</span></article>`).join("")}</section>`; }
function operacion() { const p=estado.panel || {clients:[]}; return `<div class="coach-grid">${listaClientes(p.clients || [])}${espacioCliente()}</div>`; }
function listaClientes(clientes) { return `<section class="coach-panel coach-clients"><div class="coach-panel-head"><h2>Clientes</h2><span>${clientes.length}</span></div><div class="coach-client-list">${clientes.map(c => `<button class="coach-client ${String(c.id)===String(estado.clienteId)?"active":""}" data-client-id="${html(c.id)}"><strong>${html(c.name || c.id)}</strong><span>${html(c.modality || "—")} · ${html(c.status || "—")} · Acceso ${html(c.accessStatus || "—")}</span><small>${html(c.activeWeekTitle || "Sin semana publicada")}</small>${(Number(c.pendingFeedback || 0)+Number(c.pendingCheckin || 0))>0 ? `<em>${Number(c.pendingFeedback || 0)+Number(c.pendingCheckin || 0)} pendiente(s)</em>` : ""}</button>`).join("") || `<p class="card-body">Aún no hay clientes.</p>`}</div></section>`; }
function espacioCliente() { const w=estado.espacio; if (!w) return `<section class="coach-panel coach-workspace-empty"><h2>Selecciona un cliente</h2><p>Desde aquí podrás crear semanas, sesiones, enlaces de activación, cerrar sesiones activas y revisar feedback.</p></section>`; return `<section class="coach-workspace">${resumenCliente(w)}${accesosCliente(w)}${editorSemana(w)}${editorSesion(w)}${sesionesLista(w.sessions || [])}${respuestaCliente(w)}</section>`; }
function resumenCliente(w) { const c=w.client || {}; const iri=w.iri || {}; return `<article class="coach-panel client-summary"><div><span class="section-label compact">Cliente</span><h2>${html(c.name || c.id)}</h2><p>${html(c.objective || "Objetivo no definido")}</p></div><div class="client-summary-badges"><span class="pill pill-default">${html(c.modality || "—")}</span><span class="pill pill-type">${html(c.status || "—")}</span><span class="pill pill-pub">IRI ${html(iri.total || "—")}</span></div></article>`; }
function accesosCliente(w) { const c=w.client || {}; const sesiones=w.activeSessions || []; return `<section class="coach-panel"><div class="coach-panel-head"><h2>Acceso del cliente</h2><span>${html(c.accessStatus || "—")}</span></div><div class="coach-actions-row"><button class="btn btn-primary" id="generarActivacion">Generar enlace de activación</button><button class="btn btn-ghost" id="generarRecuperacion">Recuperar contraseña</button>${sesiones.length ? `<button class="btn btn-ghost" id="revocarTodas">Cerrar sesiones activas</button>` : ""}</div><p class="card-body">El cliente elige su contraseña desde un enlace temporal. IBERFIT nunca ve ni guarda la contraseña real.</p>${sesiones.length ? `<div class="active-session-list">${sesiones.map(s => `<div class="active-session-row"><div><strong>${html(s.state || "ACTIVA")}</strong><br><small>Creada ${fecha(s.createdAt)} · vence ${fecha(s.expiresAt)}</small></div><button class="btn btn-ghost revoke-session" data-session-row="${html(s.id)}">Cerrar</button></div>`).join("")}</div>` : ""}${(w.tokens||[]).length ? `<div class="mini-table">${w.tokens.map(t => `<div><strong>${html(t.type)}</strong><span>${html(t.state)} · vence ${fecha(t.expiresAt)}</span></div>`).join("")}</div>` : ""}</section>`; }
function editorSemana(w) { const week=w.week || {}; return `<form class="coach-panel coach-form" id="weekForm"><div class="coach-panel-head"><h2>Semana</h2><span>${html(week.id || "Nueva")}</span></div><input type="hidden" id="weekId" value="${html(week.id || "")}"><div class="coach-form-grid"><label>Número<input id="weekNumber" value="${html(week.number || "")}" placeholder="1"></label><label>Estado<select id="weekState">${options(["BORRADOR","PUBLICADA","CERRADA","ARCHIVADA"], week.state || "BORRADOR")}</select></label><label>Título<input id="weekTitle" value="${html(week.title || "")}" placeholder="Semana 1 · Base técnica"></label><label>Foco<input id="weekFocus" value="${html(week.focus || "")}" placeholder="Calidad técnica y tolerancia"></label></div><label>Mensaje IBERFIT<textarea id="weekMessage" placeholder="Mensaje visible para el cliente">${html(week.message || "")}</textarea></label><button class="btn btn-primary" type="submit">Guardar semana</button></form>`; }
function bloqueForm(i) { const n=i+1; return `<article class="session-block-form"><h3>Bloque ${n}</h3><div class="coach-form-grid"><label>Nombre del bloque<input id="block${i}Title" placeholder="Preparación / Fuerza principal / Cierre"></label><label>Foco del bloque<input id="block${i}Focus" placeholder="Qué se busca en este bloque"></label></div><div class="exercise-lines">${[0,1,2,3].map(j => `<div class="exercise-line-grid"><input id="b${i}e${j}Name" placeholder="Ejercicio"><input id="b${i}e${j}Sets" placeholder="Series"><input id="b${i}e${j}Reps" placeholder="Reps"><input id="b${i}e${j}Time" placeholder="Tiempo"><input id="b${i}e${j}Rest" placeholder="Descanso"><input id="b${i}e${j}Note" placeholder="Nota técnica"></div>`).join("")}</div></article>`; }
function editorSesion(w) { const weekId=w.week?.id || ""; return `<form class="coach-panel coach-form" id="sessionForm"><div class="coach-panel-head"><h2>Crear / editar sesión</h2><span>${html(weekId || "Sin semana")}</span></div><input type="hidden" id="sessionId" value=""><div class="coach-form-grid"><label>Modalidad de sesión<select id="sessionDelivery">${options(["PRESENCIAL","AUTONOMA","ONLINE","COMPLEMENTARIA","EVALUACION"], "AUTONOMA")}</select></label><label>Tipo<select id="sessionType">${options(["Fuerza","Movilidad","Acondicionamiento","Mixta","Evaluación","Recuperación"], "Fuerza")}</select></label><label>Duración min<input id="sessionDuration" value="45"></label><label>Estado<select id="sessionState">${options(["BORRADOR","PUBLICADA","COMPLETADA","OMITIDA","REPROGRAMADA","ARCHIVADA"], "PUBLICADA")}</select></label><label>Orden<input id="sessionOrder" value="1"></label><label>Título<input id="sessionTitle" placeholder="Fuerza base tren inferior"></label></div><label>Objetivo de hoy<textarea id="sessionObjective"></textarea></label><label>Criterio IBERFIT<textarea id="sessionCriterion" placeholder="Por qué hacemos esta sesión y cómo decidir ajuste"></textarea></label><div class="coach-form-grid"><label>Preparación breve<textarea id="sessionPreparation"></textarea></label><label>Bloque principal breve<textarea id="sessionMainBlock"></textarea></label><label>Vuelta a la calma<textarea id="sessionCoolDown"></textarea></label><label>Indicaciones<textarea id="sessionIndications"></textarea></label><label>Material<input id="sessionMaterial" placeholder="Mancuernas, banda, escalón..."></label><label>Qué reportar<textarea id="sessionReport"></textarea></label></div><div class="coach-form-grid"><label>Qué observar<textarea id="sessionObserve"></textarea></label><label>Cómo ajustar<textarea id="sessionAdjust"></textarea></label></div><div class="session-block-editor"><div class="coach-panel-head"><h2>Bloques estructurados</h2><span>Visible en App Client</span></div>${[0,1,2].map(bloqueForm).join("")}</div><button class="btn btn-primary" type="submit">Guardar sesión</button></form>`; }
function sesionesLista(sesiones) { return `<section class="coach-panel"><div class="coach-panel-head"><h2>Sesiones de la semana</h2><span>${sesiones.length}</span></div><div class="coach-session-list">${sesiones.map(s => `<article class="coach-session-card"><div><strong>${html(s.title)}</strong><p>${html(s.objective || "")}</p></div><div class="session-pills"><span class="pill pill-default">${html(s.deliveryMode || "—")}</span><span class="pill pill-type">${html(s.type || "—")}</span><span class="pill pill-pub">${html(s.state || "—")}</span><span class="pill pill-dur">${html(s.duration || "—")} min</span></div><button class="btn btn-ghost coach-edit-session" data-session='${html(JSON.stringify(s))}'>Editar</button></article>`).join("") || `<p class="card-body">No hay sesiones asociadas a esta semana.</p>`}</div></section>`; }
function respuestaCliente(w) { const f=w.feedback || []; const c=w.checkin || []; return `<section class="coach-panel"><div class="coach-panel-head"><h2>Respuesta del cliente</h2><span>${f.length+c.length}</span></div><div class="coach-responses">${f.slice(0,6).map(x => tarjetaFeedback(x)).join("")}${c.slice(0,6).map(x => tarjetaCheckin(x)).join("")}${!f.length && !c.length ? `<p class="card-body">Aún no hay respuestas registradas.</p>` : ""}</div></section>`; }
function tarjetaFeedback(x) { return `<article class="coach-response-card"><span>Feedback · ${html(x.id)}</span><strong>${html(x.realization || "—")} · RPE ${html(x.rpe || "—")} · Técnica ${html(x.technique || "—")}</strong><p>${html(x.comment || "Sin comentario")}</p>${x.decision ? `<small>Decisión: ${html(x.decision)}</small>` : `<form class="response-review-form review-feedback" data-id="${html(x.id)}"><textarea placeholder="Decisión IBERFIT: mantener, progresar, ajustar o contactar"></textarea><button class="btn btn-primary" type="submit">Guardar decisión</button></form>`}</article>`; }
function tarjetaCheckin(x) { return `<article class="coach-response-card"><span>Check-in · ${html(x.id)}</span><strong>Fatiga ${html(x.fatigue || "—")} · Energía ${html(x.energy || "—")} · Molestia ${html(x.pain || "—")}</strong><p>${html(x.comment || "Sin comentario")}</p>${x.decision ? `<small>Decisión: ${html(x.decision)}</small>` : `<form class="response-review-form review-checkin" data-id="${html(x.id)}"><textarea placeholder="Decisión IBERFIT para la próxima semana"></textarea><button class="btn btn-primary" type="submit">Guardar decisión</button></form>`}</article>`; }
function altaCliente() { return `<section class="coach-panel coach-form wide-panel"><div class="coach-panel-head"><h2>Alta definitiva de cliente</h2><span>Activación segura</span></div><form id="newClientForm"><div class="coach-form-grid"><label>Nombre visible<input id="newName" placeholder="Nombre del cliente"></label><label>Correo<input id="newEmail" type="email" placeholder="cliente@email.com"></label><label>Teléfono<input id="newPhone" placeholder="+56..."></label><label>Modalidad<select id="newModality">${options(["PRESENCIAL","HIBRIDO","ONLINE"], "HIBRIDO")}</select></label><label>Estado<select id="newStatus">${options(["LEAD","DIAGNOSTICO_PENDIENTE","IRI_REALIZADO","ACTIVO","PAUSADO"], "DIAGNOSTICO_PENDIENTE")}</select></label><label>Objetivo<input id="newObjective" placeholder="Objetivo principal"></label></div><label>Observaciones<textarea id="newNotes" placeholder="Contexto operativo interno"></textarea></label><button class="btn btn-primary" type="submit">Crear cliente y enlace de activación</button></form><p class="card-body">Se crea CLIENTE_ID, acceso pendiente y enlace para que el cliente elija su contraseña.</p></section>`; }
function biblioteca() { const p=estado.panel || {}; let ejercicios=p.exerciseLibrary || []; const plantillas=p.sessionTemplates || []; const q=estado.filtroEjercicio.toLowerCase(); ejercicios=ejercicios.filter(e => (!q || String(e.name||"").toLowerCase().includes(q)) && (!estado.filtroPatron || e.pattern===estado.filtroPatron) && (!estado.filtroZona || e.zone===estado.filtroZona) && (!estado.filtroNivel || e.level===estado.filtroNivel)); return `<div class="coach-grid"><section class="coach-panel"><div class="coach-panel-head"><h2>Ejercicios</h2><span>${ejercicios.length}</span></div><div class="filter-row"><input id="filtroEjercicio" placeholder="Buscar ejercicio" value="${html(estado.filtroEjercicio)}"><select id="filtroPatron"><option value="">Patrón</option>${options(unique((p.exerciseLibrary||[]).map(e=>e.pattern)), estado.filtroPatron)}</select><select id="filtroZona"><option value="">Zona</option>${options(unique((p.exerciseLibrary||[]).map(e=>e.zone)), estado.filtroZona)}</select><select id="filtroNivel"><option value="">Nivel</option>${options(unique((p.exerciseLibrary||[]).map(e=>e.level)), estado.filtroNivel)}</select></div><form class="coach-form" id="exerciseForm"><div class="coach-form-grid"><label>Nombre<input id="exName"></label><label>Patrón<input id="exPattern" placeholder="Sentadilla, bisagra..."></label><label>Zona<input id="exZone" placeholder="Tren inferior"></label><label>Material<input id="exMaterial"></label><label>Nivel<input id="exLevel" placeholder="Base/intermedio"></label><label>Objetivo<input id="exObjective"></label></div><label>Indicaciones<textarea id="exNotes"></textarea></label><button class="btn btn-primary" type="submit">Guardar ejercicio</button></form><div class="mini-table">${ejercicios.slice(0,30).map(e => `<div><strong>${html(e.name)}</strong><span>${html(e.pattern)} · ${html(e.zone)} · ${html(e.material)}</span></div>`).join("")}</div></section><section class="coach-panel"><div class="coach-panel-head"><h2>Plantillas de sesión</h2><span>${plantillas.length}</span></div><div class="mini-table">${plantillas.map(t => `<div><strong>${html(t.name)}</strong><span>${html(t.type)} · ${html(t.deliveryMode)} · ${html(t.duration)} min</span></div>`).join("") || `<p class="card-body">Aún sin plantillas.</p>`}</div></section></div>`; }
function iriPanel() { const w=estado.espacio; const i=w?.iri || {}; return `<section class="coach-panel wide-panel"><div class="coach-panel-head"><h2>Diagnóstico IRI</h2><span>${html(i.status || "Preparado")}</span></div><p class="card-body">El IRI queda preparado como informe único: contexto, bioimpedancia, composición corporal, movilidad, fuerza, capacidad metabólica, limitadores, recomendación y decisión IBERFIT.</p><div class="iri-grid"><div>IRI ${html(i.total || "—")}</div><div>${html(i.classification || "Clasificación")}</div><div>${html(i.recommendation || "Recomendación")}</div><div>${html(i.limiters || "Limitadores")}</div><div>${html(i.bioimpedance || "Bioimpedancia")}</div><div>${html(i.mobility || "Movilidad")}</div><div>${html(i.strength || "Fuerza")}</div><div>${html(i.metabolic || "Metabólico")}</div></div></section>`; }
function seguridadPanel() { const w=estado.espacio; return `<section class="coach-panel wide-panel"><div class="coach-panel-head"><h2>Seguridad operativa</h2><span>Sesiones y accesos</span></div><p class="card-body">Desde aquí se controlan enlaces temporales y sesiones activas del cliente seleccionado. Si hay acceso indebido o dispositivo compartido, cierra las sesiones activas.</p>${w ? accesosCliente(w) : `<p class="card-body">Selecciona un cliente en Operación.</p>`}</section>`; }
function options(arr, selected) { return arr.filter(Boolean).map(v => `<option value="${html(v)}" ${String(v)===String(selected)?"selected":""}>${html(v)}</option>`).join(""); }
function unique(arr) { return [...new Set(arr.filter(Boolean))].sort(); }
async function entrar(e) { e.preventDefault(); avisar("Validando acceso..."); const res=await api("coachLogin", { login:$("#coachLogin").value.trim(), password:$("#coachPassword").value.trim() }); if (!res.ok) return avisar(res.message || "No se pudo acceder.", "error"); guardarSesion(res.data || {}); estado.panel=res.data?.dashboard || null; estado.aviso=""; render(); }
async function actualizar() { const res=await api("coachGetDashboard"); if (!res.ok) return avisar(res.message || "No se pudo actualizar.", "error"); estado.panel=res.data; estado.aviso=""; render(); }
async function cargarCliente(id) { estado.clienteId=id; avisar("Cargando cliente..."); const res=await api("coachGetClientWorkspace", { clientId:id }); if (!res.ok) return avisar(res.message || "No se pudo cargar el cliente.", "error"); estado.espacio=res.data; estado.aviso=""; render(); }
async function crearCliente(e) { e.preventDefault(); const res=await api("coachCrearCliente", { nombre:$("#newName").value.trim(), email:$("#newEmail").value.trim(), telefono:$("#newPhone").value.trim(), modalidad:$("#newModality").value, estado:$("#newStatus").value, objetivo:$("#newObjective").value.trim(), observaciones:$("#newNotes").value.trim() }); if (!res.ok) return avisar(res.message || "No se pudo crear cliente.", "error"); await actualizar(); avisar(`Cliente creado: ${res.data.clientId}. Copia el enlace de activación.`, "success", res.data.activationLink); }
async function guardarSemana(e) { e.preventDefault(); const res=await api("coachSaveWeek", { clientId:estado.clienteId, weekId:$("#weekId").value.trim(), weekNumber:$("#weekNumber").value.trim(), state:$("#weekState").value, title:$("#weekTitle").value.trim(), focus:$("#weekFocus").value.trim(), message:$("#weekMessage").value.trim() }); if (!res.ok) return avisar(res.message || "No se pudo guardar semana.", "error"); await cargarCliente(estado.clienteId); avisar("Semana guardada.", "success"); }
function leerBloques() { const blocks=[]; for (let i=0; i<3; i++) { const title=$("#block"+i+"Title")?.value.trim() || ""; const focus=$("#block"+i+"Focus")?.value.trim() || ""; const exercises=[]; for (let j=0; j<4; j++) { const name=$("#b"+i+"e"+j+"Name")?.value.trim() || ""; if (!name) continue; exercises.push({ name, sets:$("#b"+i+"e"+j+"Sets")?.value.trim() || "", reps:$("#b"+i+"e"+j+"Reps")?.value.trim() || "", time:$("#b"+i+"e"+j+"Time")?.value.trim() || "", rest:$("#b"+i+"e"+j+"Rest")?.value.trim() || "", note:$("#b"+i+"e"+j+"Note")?.value.trim() || "" }); } if (title || focus || exercises.length) blocks.push({ title:title || `Bloque ${i+1}`, focus, exercises }); } return blocks; }
async function guardarSesion(e) { e.preventDefault(); const blocks=leerBloques(); const res=await api("coachSaveSession", { clientId:estado.clienteId, weekId:estado.espacio?.week?.id || $("#weekId")?.value || "", sessionId:$("#sessionId").value.trim(), deliveryMode:$("#sessionDelivery").value, type:$("#sessionType").value, duration:$("#sessionDuration").value.trim(), state:$("#sessionState").value, order:$("#sessionOrder").value.trim(), title:$("#sessionTitle").value.trim(), objective:$("#sessionObjective").value.trim(), criterion:$("#sessionCriterion").value.trim(), preparation:$("#sessionPreparation").value.trim(), mainBlock:$("#sessionMainBlock").value.trim(), coolDown:$("#sessionCoolDown").value.trim(), indications:$("#sessionIndications").value.trim(), material:$("#sessionMaterial").value.trim(), observe:$("#sessionObserve").value.trim(), adjust:$("#sessionAdjust").value.trim(), report:$("#sessionReport").value.trim(), blocksJson:JSON.stringify(blocks) }); if (!res.ok) return avisar(res.message || "No se pudo guardar sesión.", "error"); await cargarCliente(estado.clienteId); avisar("Sesión guardada y sincronizada con App Client.", "success"); }
async function generarEnlace(tipo) { if (!estado.clienteId) return; const action=tipo==="reset" ? "coachGenerarEnlaceRecuperacion" : "coachGenerarEnlaceActivacion"; const res=await api(action, { clientId:estado.clienteId }); if (!res.ok) return avisar(res.message || "No se pudo generar enlace.", "error"); const link=res.data.resetLink || res.data.activationLink; await cargarCliente(estado.clienteId); avisar(tipo==="reset" ? "Enlace de recuperación generado." : "Enlace de activación generado.", "success", link); }
async function guardarEjercicio(e) { e.preventDefault(); const res=await api("coachGuardarEjercicio", { nombre:$("#exName").value.trim(), patron:$("#exPattern").value.trim(), zona:$("#exZone").value.trim(), material:$("#exMaterial").value.trim(), nivel:$("#exLevel").value.trim(), objetivo:$("#exObjective").value.trim(), indicaciones:$("#exNotes").value.trim() }); if (!res.ok) return avisar(res.message || "No se pudo guardar ejercicio.", "error"); await actualizar(); avisar("Ejercicio guardado en biblioteca.", "success"); }
async function guardarDecision(tipo,id,decision) { const action=tipo==="feedback" ? "coachReviewFeedback" : "coachReviewCheckin"; const payload=tipo==="feedback" ? { feedbackId:id, decision } : { checkinId:id, decision }; const res=await api(action, payload); if (!res.ok) return avisar(res.message || "No se pudo guardar decisión.", "error"); await cargarCliente(estado.clienteId); avisar("Decisión IBERFIT guardada.", "success"); }
async function revocarSesion(id) { const res=await api("coachRevocarSesion", { clientId:estado.clienteId, sessionAccessId:id }); if (!res.ok) return avisar(res.message || "No se pudo cerrar la sesión.", "error"); await cargarCliente(estado.clienteId); avisar("Sesión cerrada desde servidor.", "success"); }
async function revocarTodas() { const res=await api("coachRevocarSesionesCliente", { clientId:estado.clienteId }); if (!res.ok) return avisar(res.message || "No se pudieron cerrar las sesiones.", "error"); await cargarCliente(estado.clienteId); avisar("Sesiones activas cerradas.", "success"); }
function rellenarBloques(s) { let blocks=[]; try { blocks=JSON.parse(s.blocksJson || "[]"); } catch { blocks=[]; } blocks.slice(0,3).forEach((b,i) => { $("#block"+i+"Title").value=b.title || ""; $("#block"+i+"Focus").value=b.focus || ""; (b.exercises || []).slice(0,4).forEach((ex,j) => { $("#b"+i+"e"+j+"Name").value=ex.name || ""; $("#b"+i+"e"+j+"Sets").value=ex.sets || ""; $("#b"+i+"e"+j+"Reps").value=ex.reps || ""; $("#b"+i+"e"+j+"Time").value=ex.time || ""; $("#b"+i+"e"+j+"Rest").value=ex.rest || ""; $("#b"+i+"e"+j+"Note").value=ex.note || ""; }); }); }
function rellenarSesion(raw) { try { const s=JSON.parse(raw); $("#sessionId").value=s.id || ""; $("#sessionDelivery").value=s.deliveryMode || "AUTONOMA"; $("#sessionType").value=s.type || "Fuerza"; $("#sessionDuration").value=s.duration || ""; $("#sessionState").value=s.state || "PUBLICADA"; $("#sessionOrder").value=s.order || ""; $("#sessionTitle").value=s.title || ""; $("#sessionObjective").value=s.objective || ""; $("#sessionCriterion").value=s.criterion || ""; $("#sessionPreparation").value=s.preparation || ""; $("#sessionMainBlock").value=s.mainBlock || s.structure || ""; $("#sessionCoolDown").value=s.coolDown || ""; $("#sessionIndications").value=s.indications || ""; $("#sessionMaterial").value=s.material || ""; $("#sessionObserve").value=s.observe || ""; $("#sessionAdjust").value=s.adjust || ""; $("#sessionReport").value=s.report || ""; rellenarBloques(s); window.scrollTo({ top:$("#sessionForm").offsetTop - 24, behavior:"smooth" }); } catch {} }
function bind() { $("#coachLoginForm")?.addEventListener("submit", entrar); $("#coachRefreshBtn")?.addEventListener("click", actualizar); $("#coachLogoutBtn")?.addEventListener("click", async () => { try { await api("coachLogout"); } catch {} sessionStorage.removeItem(COACH_KEY); estado.token=""; estado.panel=null; estado.espacio=null; render(); }); $$(".coach-tab").forEach(b => b.addEventListener("click", () => { estado.pestaña=b.dataset.tab; render(); })); $$(".coach-client").forEach(b => b.addEventListener("click", () => cargarCliente(b.dataset.clientId))); $("#newClientForm")?.addEventListener("submit", crearCliente); $("#weekForm")?.addEventListener("submit", guardarSemana); $("#sessionForm")?.addEventListener("submit", guardarSesion); $("#generarActivacion")?.addEventListener("click", () => generarEnlace("act")); $("#generarRecuperacion")?.addEventListener("click", () => generarEnlace("reset")); $("#revocarTodas")?.addEventListener("click", revocarTodas); $$(".revoke-session").forEach(b => b.addEventListener("click", () => revocarSesion(b.dataset.sessionRow))); $("#exerciseForm")?.addEventListener("submit", guardarEjercicio); $$(".coach-edit-session").forEach(b => b.addEventListener("click", () => rellenarSesion(b.dataset.session))); $$(".review-feedback").forEach(f => f.addEventListener("submit", e => { e.preventDefault(); guardarDecision("feedback", f.dataset.id, $("textarea", f).value.trim()); })); $$(".review-checkin").forEach(f => f.addEventListener("submit", e => { e.preventDefault(); guardarDecision("checkin", f.dataset.id, $("textarea", f).value.trim()); })); $("#copyLastLink")?.addEventListener("click", async () => { try { await navigator.clipboard.writeText(estado.enlace); avisar("Enlace copiado.", "success", estado.enlace); } catch {} }); $("#filtroEjercicio")?.addEventListener("input", e => { estado.filtroEjercicio=e.target.value; render(); }); $("#filtroPatron")?.addEventListener("change", e => { estado.filtroPatron=e.target.value; render(); }); $("#filtroZona")?.addEventListener("change", e => { estado.filtroZona=e.target.value; render(); }); $("#filtroNivel")?.addEventListener("change", e => { estado.filtroNivel=e.target.value; render(); }); }
function render() { root.innerHTML = estado.token ? shell() : loginPantalla(); bind(); }
async function init() { const s=leerSesion(); if (s) { estado.token=s.coachToken; estado.vence=s.expiresAt; estado.coach=s.coach || {}; const res=await api("coachGetDashboard"); if (res.ok) estado.panel=res.data; else sessionStorage.removeItem(COACH_KEY); } render(); }
init();
